.TH _PRINTF 3 "November 2025" "Holberton School" "Library Functions Manual"
.SH NAME
_printf \- formatted output conversion

.SH SYNOPSIS
.B #include "main.h"
.PP
.BI "int _printf(const char *" format ", ...);"

.SH DESCRIPTION
_Brief description here..._
.TH _PRINTF 3 "November 2025" "Holberton School" "Library Functions Manual"
.SH NAME
_printf \- formatted output conversion

.SH SYNOPSIS
.B #include "main.h"
.PP
.BI "int _printf(const char *" format ", ...);"

.SH DESCRIPTION
.B _printf
produces output according to a format string and a variable number of arguments.
It writes the output to the standard output (file descriptor 1).

The
.I format
string is composed of zero or more ordinary characters and conversion
specifications. Ordinary characters are printed as they are.

A conversion specification begins with the character
.B %
and is followed by a conversion specifier character that indicates
how to convert the corresponding argument.

This implementation supports the following conversion specifiers:

.TP
.B %c
Prints a single character.
The corresponding argument is an
.B int
which is converted to an unsigned char.

.TP
.B %s
Prints a null-terminated string.
The corresponding argument is a pointer to
.B char
\&. If the argument is
.BR NULL ,
the string
.RB "(null)"
is printed.

.TP
.B %%
Prints a literal
.B %
character (no argument is consumed).

.TP
.B %d
Prints a signed decimal integer.
The corresponding argument is an
.B int
\&. Negative numbers are printed with a leading minus sign.

.TP
.B %i
Same as
.BR %d .

.PP
This implementation does not handle flag characters, field width,
precision, or length modifiers.

.SH RETURN VALUE
On success,
.B _printf
returns the total number of characters printed.

If
.I format
is
.BR NULL ,
or if an error occurs while writing to the standard output,
.B _printf
returns
.BR -1 .

.SH EXAMPLES
.nf
_printf("Hello, %s\\n", "world");
    /* Prints: Hello, world\n */

_printf("Character: %c\\n", 'A');
    /* Prints: Character: A\n */

_printf("Number: %d, %i\\n", 1024, -1024);
    /* Prints: Number: 1024, -1024\n */

_printf("Percent: %%\\n");
    /* Prints: Percent: %\n */
.fi

.SH AUTHORS
Written by students of Holberton School.
